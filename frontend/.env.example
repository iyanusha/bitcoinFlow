# Network: "testnet" or "mainnet"
VITE_NETWORK=testnet

# Contract deployer address (defaults to testnet deployer if not set)
VITE_CONTRACT_ADDRESS=ST1PQHQKV0RJXZFY1DGX8MNSNYVE3VGZJSRTPGZGM

# Note: All environment variables must be prefixed with VITE_ to be
# exposed to the frontend bundle. Never put secrets here — this file
# is compiled into the client-side JavaScript bundle.
